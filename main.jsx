import React,{useState} from 'react';import{createRoot}from'react-dom/client';import ReactMarkdown from'react-markdown';import'./style.css';
function App(){const[m,setM]=useState([{role:'assistant',content:'Hello! I am your AI assistant. Ask me anything.'}]);const[v,setV]=useState('');const[busy,setBusy]=useState(false);
async function send(){if(!v.trim()||busy)return;let text=v.trim();setV('');let next=[...m,{role:'user',content:text}];setM(next);setBusy(true);try{let r=await fetch('http://localhost:4000/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:next})});let d=await r.json();setM(x=>[...x,{role:'assistant',content:d.reply||'No response'}]);}catch(e){setM(x=>[...x,{role:'assistant',content:'Backend is not running. Start the backend with npm run dev.'}]);}setBusy(false)}
return <div className="app"><aside><button onClick={()=>setM([{role:'assistant',content:'New chat started.'}])}>＋ New chat</button><h3>My AI</h3><p>ChatGPT-like starter</p></aside><main><header>AI Assistant</header><section>{m.map((x,i)=><div className={'msg '+x.role} key={i}><b>{x.role==='user'?'You':'AI'}</b><ReactMarkdown>{x.content}</ReactMarkdown></div>)}{busy&&<div className="msg assistant"><b>AI</b><p>Thinking…</p></div>}</section><footer><textarea value={v} onChange={e=>setV(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="Message AI…"/><button onClick={send}>↑</button></footer></main></div>}createRoot(document.getElementById('root')).render(<App/>);